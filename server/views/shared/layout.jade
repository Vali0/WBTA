doctype
html
    head
        title Google PageSpeed System
        link(rel="stylesheet" type="text/css" href="/lib/bootstrap-3.2.0/css/bootstrap.min.css")
        link(rel="stylesheet" type="text/css" href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css")
        link(rel="stylesheet" type="text/css" href="/css/site.css")
    body
        header.nav
        .container
            .navbar-header
                a.navbar-brand(href="/") Page Speed
            .navbar-collapse.collapse
                ul.nav.navbar-nav
                    li
                        a(href="/") Home
                    if (currentUser)
                        li
                            a(href="/page-speed") Page Speed
                        li
                            a(href="/benchmark-results") Benchmark Results
                        li
                            a(href="/profile") #{currentUser.username}
                        //- somewhy in currentUser is setted whole user model, Check why so because it carry all user information(hash:salt)
                        li
                            a(href="/logout") Logout
                    else
                        li
                            a(href="/register") Register
                        li
                            a(href="/login") Login

        .container
            if (errorMessage)
                .alert.alert-danger #{errorMessage}

            block main-content

        footer
            p.text-center
                | &copy; Westum Bulgaria 2015

        include scripts